#!/bin/bash
# Fake `pkexec` for tests: runs the given command directly, no polkit
# agent/auth prompt involved. Resolves against the fixtures PATH just like
# a real shell would, so a faked target (pacman, ufw, ...) is what actually
# runs. Never touches the real system, never opens a real GUI prompt.
exec "$@"
